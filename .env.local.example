# --- Render の Environment にコピーする値（.env.local はコミットしない）---

# ログイン画面「パスワードをお忘れの方」に表示する連絡先
NEXT_PUBLIC_ADMIN_CONTACT_EMAIL=you@example.com

# Supabase（ダッシュボード → Project Settings → API）
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key

# サーバー専用（ブラウザに出さない）。未解決BOXの status 同期などで使用
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-service-role-key

# OpenAI（チャット・埋め込み）
OPENAI_API_KEY=sk-your-openai-api-key

# ElevenLabs（読み上げ TTS）
# - API キーだけ必須。ボイスを削除した場合、古い VOICE_ID は使えない
# - VOICE_ID を空にするとプリセット音声（Rachel）を使用
# - 別の声にしたい: ElevenLabs → Voices → 使う声の Voice ID をコピー
# - 音声不要なら API キーを空のまま（チャットのスピーカーOFFでも可）
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
# Eleven v3（推奨）/ 低レイテンシ: eleven_flash_v2_5 / バランス: eleven_turbo_v2_5
ELEVENLABS_MODEL_ID=eleven_v3
# 読み上げ速度（1.0=標準、1.56=約1.56倍。さらに速くする場合は 1.7 など）
ELEVENLABS_TTS_SPEED=1.56
